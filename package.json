{
  "name": "chronicles-rebuild",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "build": "npm run build --workspace=shared && npm run generate:prisma && npm run build --workspace=server && npm run build --workspace=client",
    "start": "node server/dist/index.js",
    "generate:prisma": "cd server && npx prisma generate",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "test": "npm run test --workspace=shared && npm run test --workspace=server && npm run test --workspace=client",
    "test:watch": "concurrently \"npm run test:watch --workspace=shared\" \"npm run test:watch --workspace=server\" \"npm run test:watch --workspace=client\"",
    "test:coverage": "npm run test:coverage --workspace=shared && npm run test:coverage --workspace=server && npm run test:coverage --workspace=client",
    "migrate:tenants": "npm run migrate:tenants --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@fortawesome/free-regular-svg-icons": "^7.2.0"
  }
}
