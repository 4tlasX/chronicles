CRITICAL: All UI implementation MUST follow the design system in `/design-system/` — the runnable mini-app at `/design-system/Chronicles Desktop App/` (app.jsx + index.html), the binding rules in `/design-system/CLAUDE.md`, and the token source `/design-system/tokens/*.css`. Use the DS semantic CSS variables (`--bg-*`, `--text-*`, `--border-*`, `--color-accent*`, `--font-display`, `--font-sans`, `--font-label`) injected in `client/src/App.tsx`. The old `/design_handoff/chronicles-design-system.html` paper/serif spec is retired.
