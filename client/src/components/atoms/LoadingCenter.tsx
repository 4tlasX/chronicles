import styled from 'styled-components';

/** Flex container that centers its children both vertically and horizontally. */
export const LoadingCenter = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 1;
`;
